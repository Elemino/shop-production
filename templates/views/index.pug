extends ../layouts/default

block content
	.container: .jumbotron
		img(src='/images/logo.svg', width='160')
		h1 Welcome
	
		p Currently, logging in will take you to the<a href='http://keystonejs.com' target='_blank'> Keystone</a> Admin  website.
		p Go back to elemshalit.com <a href='http://elemshalit.com' target='_blank'> here.</a>
		hr
		if user && user.canAccessKeystone
			p
				a(href='/keystone').btn.btn-lg.btn-primary Open the Admin UI
		else
			
			p
				
		hr


		
